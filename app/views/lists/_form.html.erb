<%= simple_form_for(@list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label:'單據名稱', required: true %>
    <%= f.input :serial, label:'單據編號', required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "確認" %>
  </div>
<% end %>
